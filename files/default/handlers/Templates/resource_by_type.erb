==========================================
Resource Summary Report (by resource type)
==========================================
<% @data.each_pair do |resource_group, resources| %>
<%= resource_group %>
<% resources.each do |resource| %>
<%= "%2s %-45s %s" % ['', resource, resource.updated ? '(updated)' : ''] %>
<% end %>

<% end %>
